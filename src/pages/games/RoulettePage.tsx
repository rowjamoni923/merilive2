import { RouletteGame } from "@/features/games/roulette";

/**
 * Roulette Game Page
 * Uses the built-in React roulette game connected to the app's coin system
 */
export default function RoulettePage() {
  return <RouletteGame />;
}
