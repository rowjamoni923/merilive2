import 'package:flutter/material.dart';

import '../../reels/screens/reels_feed_page.dart';

/// Reels tab entry — R2 skeleton mounts the vertical feed page.
class ReelsTabPage extends StatelessWidget {
  const ReelsTabPage({super.key});

  @override
  Widget build(BuildContext context) => const ReelsFeedPage();
}
