<?xml version="1.0" encoding="utf-8"?>
<!--
  M13 — Full-screen incoming call layout used by IncomingCallActivity
  (the killed-app / lock-screen ringer that fires before Flutter is ready).

  When Flutter is running, the Dart `IncomingCallPage` is the primary UI —
  this layout only shows for cold-start / background wake, and it hands
  off to Flutter via the broadcast bridge as soon as the user accepts.

  Kept intentionally minimal (system look) so it never diverges from
  the Dart ringer visually — the design source of truth is Flutter side.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B0B12"
    android:fitsSystemWindows="true">

    <ImageView
        android:id="@+id/caller_avatar_bg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:alpha="0.35"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/call_type_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#E5E7EB"
        android:letterSpacing="0.1"
        android:textSize="13sp"
        android:layout_marginTop="80dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Incoming Video Call" />

    <TextView
        android:id="@+id/caller_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/call_type_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Caller Name" />

    <View
        android:id="@+id/decline_call_button"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:background="#EF4444"
        android:layout_marginBottom="96dp"
        android:layout_marginStart="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <View
        android:id="@+id/accept_call_button"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:background="#22C55E"
        android:layout_marginBottom="96dp"
        android:layout_marginEnd="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
