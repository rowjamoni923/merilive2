<?xml version="1.0" encoding="utf-8"?>
<!--
    ============================================
    MeriLive Android — সমস্ত Permissions
    ============================================
    এই permissions গুলো AndroidManifest.xml এ
    <manifest> ট্যাগের ভিতরে, <application> এর আগে যোগ করুন
    ============================================
-->

<!-- ========== Internet & Network ========== -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

<!-- ========== Camera ========== -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="false" />
<uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
<uses-feature android:name="android.hardware.camera.front" android:required="false" />
<uses-feature android:name="android.hardware.camera.flash" android:required="false" />

<!-- ========== Microphone / Audio ========== -->
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-feature android:name="android.hardware.microphone" android:required="false" />

<!-- ========== Location ========== -->
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<!-- REMOVED: ACCESS_BACKGROUND_LOCATION — Google Play rejected (Missing Prominent Disclosure) -->
<uses-feature android:name="android.hardware.location" android:required="false" />
<uses-feature android:name="android.hardware.location.gps" android:required="false" />
<uses-feature android:name="android.hardware.location.network" android:required="false" />

<!-- ========== Storage (Android 12 and below) ========== -->
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
<!-- REMOVED: MANAGE_EXTERNAL_STORAGE - Google Play rejected (All Files Access policy) -->

<!-- ========== Storage (Android 13+ Media Picker) ========== -->
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
<uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
<uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

<!-- ========== Notifications (Android 13+) ========== -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- ========== Vibration & Haptics ========== -->
<uses-permission android:name="android.permission.VIBRATE" />

<!-- ========== Phone / Call / Telephony ========== -->
<!-- REMOVED: READ_PHONE_STATE / CALL_PHONE / READ_CALL_LOG / ANSWER_PHONE_CALLS / MANAGE_OWN_CALLS -->
<!-- Reason: Google Play SMS/Call Log policy rejection risk.
     MeriLive calls run on LiveKit WebRTC, not Android telecom APIs. -->

<!-- ========== Contacts ========== -->
<uses-permission android:name="android.permission.READ_CONTACTS" />
<uses-permission android:name="android.permission.WRITE_CONTACTS" />

<!-- ========== Wake Lock & Foreground Services ========== -->
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

<!-- ========== Full Screen & Overlay ========== -->
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
<uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

<!-- ========== Boot & Background ========== -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

<!-- ========== Bluetooth (Audio Devices) ========== -->
<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
<uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
<uses-feature android:name="android.hardware.bluetooth" android:required="false" />

<!-- ========== Biometrics / Fingerprint ========== -->
<uses-permission android:name="android.permission.USE_BIOMETRIC" />
<uses-permission android:name="android.permission.USE_FINGERPRINT" />
<uses-feature android:name="android.hardware.fingerprint" android:required="false" />

<!-- ========== NFC (Optional) ========== -->
<uses-permission android:name="android.permission.NFC" />
<uses-feature android:name="android.hardware.nfc" android:required="false" />

<!-- ========== Billing / In-App Purchase ========== -->
<uses-permission android:name="com.android.vending.BILLING" />

<!-- ========== Install Packages ========== -->
<!-- REMOVED: REQUEST_INSTALL_PACKAGES — Google Play policy rejection risk -->

<!-- ========== Scheduled Alarms (Reminders/Timers) ========== -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_EXACT_ALARM" />

<!-- ========== Clipboard ========== -->
<!-- Android 13+ clipboard access is automatic, no permission needed -->

<!-- ========== Sensors ========== -->
<!-- REMOVED: BODY_SENSORS — Google Play policy rejection risk (health declaration required) -->
<uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
<uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
<uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

<!-- ========== DeepAR SDK — Android 12+ OpenCL (REQUIRED) ========== -->
<!-- DeepAR Background Segmentation ও কিছু Effect-এ OpenCL লাগে।
     এটা ছাড়া Android 12+ এ crash/black screen হয়। -->
<uses-native-library android:name="libOpenCL.so" android:required="false" />
<!-- Pixel ডিভাইসের জন্য আলাদা OpenCL library -->
<uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />
