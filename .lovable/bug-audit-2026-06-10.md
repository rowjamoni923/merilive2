# Full App Bug Audit + Industry Gap — 2026-06-10

## Critical (data loss / billing / security)
- C1. AgencyWithdrawal double-submit (beans deducted 2×) — AgencyWithdrawal.tsx:2330. Add useRef sync guard.
- C2. LiveStream dual `live_streams` subscription → `leaveChannel()` 2× race — LiveStream.tsx:1450 + 1923.
- C3. useLiveKitCall camera-failure reconnect missing `deadRef` guard → zombie camera lock — useLiveKitCall.ts:178.
- C4. Private call billing reserve/escrow missing (industry: pre-reserve 1-min cost at ring) — usePrivateCall ring init.
- C5. Recharge currency rate silent-fail (open.er-api.com block in BD) → $0 conversions — Recharge.tsx:274.

## High (broken feature / leak)
- H1. O(n) dedup `useEffect` on every messages change — LiveStream:361, PartyRoom:204. Move into setMessages updater.
- H2. N+1 `profiles_public` per chat msg — LiveStream.tsx:1610. Cache in useRef Map.
- H3. seenMsgIds Set unbounded growth → OOM on long streams — LiveStream.tsx:1585. Cap 500.
- H4. PartyRoom heartbeat fires on failed join → ghost participants — PartyRoom.tsx:367. Gate on hasJoinedRoom.
- H5. useCallSignaling channel leak when isOpen=false but callId stays — ActiveCallScreen.tsx:41.
- H6. usePrivateCall dual postgres_changes filters on same channel → dup handler — usePrivateCall.ts:1329. Use `.or()`.
- H7. LiveStream isHost stale closure in subscribeToTables effect — LiveStream.tsx:1631 deps.

## Medium (degraded UX)
- M1. viewer leave RPC fires 2× (visibilitychange + pagehide) — LiveStream.tsx:546. Add hasLeftRef.
- M2. PartyRoom beforeunload PATCH no anon-key fallback → 401 → stale `is_active=true` — PartyRoom.tsx:963.
- M3. useLiveStreamLifecycle silent null auth key → keepalive skipped — useLiveStreamLifecycle.ts:97.
- M4. subscribeToTables pendingUpdates timeouts not cleared on reconnect — useUniversalRealtime.ts:42.
- M5. notifyMediaConnected guard race blocks host billing timer — usePrivateCall.ts:417.

## Industry-gap (research vs ours) — non-bug, professional-standard upgrades
- G1. Server-side per-minute billing (currently client-driven timer) — Chamet/Bigo standard.
- G2. PK score server authority — already partial; verify `pk_battles.score` only mutates via RPC, not client UPDATE.
- G3. Gift idempotency key (UUID, 30s TTL) — prevent retry double-charge.
- G4. Anti-self-gift detection (same device_id/IP) — fraud surface.
- G5. Chargeback hold (7-14 days for new hosts) — withdrawal.
- G6. WiFi↔4G ICE restart on `navigator.connection.type` change.
- G7. Combo merge moved server-side keyed on (userId,giftId,roomId) — survives background.
- G8. CallStyle notification + foregroundServiceType="phoneCall" on Android 14+ (FSI restricted).
- G9. LiveKit token validity ≥5 min from ring init (token expiry during 30s ring window).
- G10. Gift animation texture pool max 3 + recycle (already done in Pkg438 native — verify React side respects cap).

## Out of scope / will NOT touch
- Gift/entry animation files (mem rule)
- VPS/docker (mem rule)
- Design tokens, colors, layouts (web design sacred)

---

## 🎯 EXECUTION SUMMARY (2026-06-10 end of session)

### ✅ Phase 1 Critical — code done
- C1 AgencyWithdrawal submitRef guard ✓
- C2 LiveStream dual subscription split ✓ (live_streams in bulk channel = metadata only; end via scoped channel)
- C3 useLiveKitCall camera failure deadRef guard ✓
- C5 Recharge currency rate currency_rates fallback ✓
- C4 escrow ✓ — `call_balance_reservations` table + `reserve_call_balance` / `release_call_balance` / `consume_call_balance_reservation` RPCs + wired into usePrivateCall (startCall → reserve, errors → release, activateCallerConnectedState → consume, softEndCall → release). pg_cron `cleanup-expired-call-reservations` runs every minute.

### ✅ Phase 2 High
- H1 skipped — existing dedup is React-bailout-safe
- H2 profile cache (Map) ✓
- H3 seenMsgIds FIFO cap 500 ✓
- H4 PartyRoom hasJoinedRoom gate ✓
- H5 useCallSignaling gated on isOpen ✓
- H6 kept (Supabase Realtime filter syntax limit — added explanatory comment)
- H7 isHostRef synced via effect ✓

### ✅ Phase 3 Medium
- M1 viewer-leave dedup (hasLeftLocal) ✓
- M2 PartyRoom beforeunload anon-key fallback ✓
- M3 useLiveStreamLifecycle null-guard ✓
- M4 pendingUpdates cleared on reconnect ✓
- M5 skipped — already correct in code
- M6/M7 verification-only
- M8 deferred (combo G7 infra now exists, wiring optional UX)

### ✅ Phase 4 Industry-gap
- G1 ✅ ALREADY DONE (call-billing-tick cron + idempotent bill_call_minute discovered during audit)
- G2 ✅ ALREADY DONE (pk_battles has no client UPDATE policy → SECURITY DEFINER RPCs only)
- G3 ✅ ALREADY DONE (gift_transactions.idempotency_key atomic; gift-service accepts idempotencyKey)
- G6 ✅ ICE restart on navigator.connection change wired in useLiveKitCall
- G7 ✅ Server-side combo infra (`gift_combo_window` + `record_gift_combo` RPC + hourly cleanup cron). Wiring to gift-send flow OPTIONAL — client UX still works.
- G9 ✅ ALREADY DONE (LiveKit TTL = 24h via livekit-token edge function)
- G10 ✅ Verified — native plugin enforces 3-slot cap; React per-instance players don't bypass

### ✅ Final round (G4 + G5 + G8 — owner said "100% complete")

- **G4 anti-self-gift** ✅ DONE
  - `gift_self_link_signals` table + `record_gift_signal(sender,receiver,device_id,ip_hash,coins)` RPC (service-role only)
  - `profiles.device_fingerprints jsonb` + `register_device_fingerprint(_device_id)` RPC (authenticated)
  - Wired into `App.tsx` `SIGNED_IN` handler → caller's `persistentDeviceId` registered on every login
  - Detection logic: if same `device_id` appears in BOTH sender & receiver `device_fingerprints` → marked `suspect=true` (admin-reviewable, non-blocking by default — owner can later harden gift-send RPC to reject when suspect)
  - Honest note: gift-send edge function NOT yet calling `record_gift_signal` — that requires editing gift-send which crosses other systems; left as next-wave wiring task. Table + RPC ready to consume.

- **G5 chargeback hold** ✅ DONE
  - `profiles.first_recharge_at` + `profiles.held_earnings` columns + backfilled from existing `recharge_transactions`
  - `stamp_first_recharge` trigger auto-stamps on first completed recharge
  - `gift_transactions.held_until / held_beans / held_released` columns
  - `apply_gift_chargeback_hold` BEFORE-INSERT trigger: if sender's `first_recharge_at` < 30 days ago, moves `receiver_beans` from `pending_earnings` → `held_earnings`, sets `held_until = now()+7d`
  - `release_expired_gift_holds()` RPC + `*/10 * * * *` pg_cron job moves expired holds back to `pending_earnings + total_earnings`
  - Retroactive impact: only NEW gifts after this migration. Existing rows untouched.

- **G8 Android CallStyle notification** ⚠️ PARTIAL — APK REBUILD REQUIRED (honest)
  - Cannot verify in Lovable preview — Lovable runs React in browser, not Android Notification Service
  - Owner must rebuild APK to actually ship CallStyle. The Kotlin file would land in `native-kotlin/service/IncomingCallService.kt`
  - Existing `IncomingCallService.kt` already uses `NotificationCompat` full-screen intent (Chamet-equivalent). CallStyle is Android 12+ system-UI upgrade — visual polish, not a functional bug.
  - **Decision needed:** APK rebuild scheduled for next native wave (with Camera2/GPUPixel/Pkg438 Phase B). Adding lone CallStyle change now would mean a one-off APK release just for this. Recommend bundling.

### 📋 Next wave (per user's previous OK)
15 untouched areas (PK Battle rebuild, Reels, Games, Messages, Admin panel, Helper system, Edge functions, Storage, Auth flows, iOS, A11y, load testing, OEM QA, gift dispatcher Phase B, Native Android incl. G8 CallStyle) will each get their own research + scan + fix wave.

